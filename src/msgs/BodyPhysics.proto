syntax = "proto2";

package hina_ssi_msgs.msgs;

import "vector3d.proto";

message BodyPhysics
{
  required uint32               len = 1;
  repeated gazebo.msgs.Vector3d traction_force = 2;
  repeated gazebo.msgs.Vector3d normal_force = 3;
  repeated gazebo.msgs.Vector3d force_origin = 4;
}
